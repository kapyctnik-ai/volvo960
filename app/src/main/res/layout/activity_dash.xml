<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0B0D0E">

    <com.volvo960.obdctl.ui.dash.DashboardView
        android:id="@+id/dashboard"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Only surfaces when something is wrong; the cluster stays clean otherwise. -->
    <TextView
        android:id="@+id/textDashStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="6dp"
        android:alpha="0.75"
        android:textColor="#7FF2F4"
        android:textSize="11sp"
        android:visibility="gone" />

    <!-- Live traffic, shown while no reading has arrived: without seeing the
         actual exchange there is no way to tell an unsent request from an
         ignored one. Hides itself as soon as a gauge has a value. -->
    <TextView
        android:id="@+id/textDebugLog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_margin="8dp"
        android:background="#B0000000"
        android:fontFamily="monospace"
        android:padding="6dp"
        android:textColor="#9BF7F9"
        android:textSize="9sp"
        android:visibility="gone" />
</FrameLayout>
